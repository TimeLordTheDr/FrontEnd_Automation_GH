html
    head
        script(src="/assets/js/lib.min.js")
        link(rel="stylesheet" href="css/application.min.css")
    body
        p Hello
        script. //WE USE 'script.' TO RENDER INLINE JS
            'use strict';
            /*$(window).on('load', function () {
                alert("window load occurred!");
            });*/ //WE USE document.ready INSTEAD SINCE IT ENSURES FULL PAGE LOAD BETTER
            function User(firstName, lastName) {
                var phrase = "Привет";
                var firstName = 'Uasya' || firstName;
                var lastName = lastName || 'Piterskiy';

                function getFullName() {
                    return firstName + " " + lastName;
                }

                this.sayHi = function () {
                    alert(phrase + ", " + getFullName());
                };
            }
            var vasya = new User(undefined, "Петров");
            vasya.sayHi();
            $('p').draggable();
        script.
            $(document).ready(function () {
               $.getScript("../dist/js/app-browserify.min.js");
            });